# Dashboard environment variables (EXAMPLE)
# Copy this file to dashboard.env and fill with your actual values

# Python API URL (usa localhost per dev locale, python-api per Docker)
API_BASE_URL=http://localhost:5001

# Rust Engine URL (usa localhost per dev locale, rust-engine per Docker)
RUST_ENGINE_URL=http://localhost:8080